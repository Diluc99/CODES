    #include <iostream>
    using namespace std;
    int main()
    {
        
        bool exp1=true;
        bool exp2=false;
    cout <<(exp1&&exp2)<<endl;  //false   iff both exp1 and exp2 are true then it will retuen true9(logical and)
    cout <<(exp1||exp2)<<endl; //true     if either or both exp1 and exp2 are true then it will retuen true9(logical or) 
    cout <<(!exp1);            //false    if exp1 is true then it will retuenn flase and vice versa(logical not)

        return 0;
    }